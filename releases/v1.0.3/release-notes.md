# KVIK TV v1.0.3

This release adds an API-controlled `PODRSKA` tile on the main screen, localizes ORPlayer weekday and month labels to Croatian, defaults app-owned time displays to 24-hour format, and updates HTTP client identification to `KVIK TV App`.

The support tile remains remotely controlled through the preserved compatibility key `button.btn_account`.
